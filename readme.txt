A seed project for using React and Redux with ASP.NET API. 
Navigation with React-Router.

Compiled with Webpack.

comboserve.js: redirects api calls to iis server (set DEBUG=true for developer mode)
